# lesson1_syntax.py

"""
Workshop 1: Python Syntax Basics
Topic: Variables (ตัวแปร), Type Hinting (ระบุประเภทข้อมูล), และ f-strings
Author: Coding Partner & You
"""

import datetime # เรียกใช้เครื่องมือจัดการเวลาที่มีมากับ Python

def calculate_age(birth_year: int) -> int:
    """
    ฟังก์ชันคำนวณอายุ
    Args:
        birth_year (int): ปีเกิด (ค.ศ.)
    Returns:
        int: อายุที่คำนวณได้
    """
    # ดึงปีปัจจุบัน (เช่น 2025)
    current_year: int = datetime.datetime.now().year
    
    # คำนวณอายุ (ปีปัจจุบัน - ปีเกิด)
    age: int = current_year - birth_year
    
    return age

# --- Main Entry Point (จุดเริ่มทำงานของโปรแกรม) ---
if __name__ == "__main__":
    print("--- 🟢 โปรแกรมคำนวณอายุ (Clean Code) ---")

    # 1. การประกาศตัวแปรแบบระบุประเภท (Type Hinting)
    # format คือ -> ชื่อตัวแปร: ประเภท = ค่าที่เก็บ
    my_name: str = "Developer M1"
    my_birth_year: int = 1995    # ลองเปลี่ยนเป็นปีเกิดของคุณดูนะครับ
    is_ready_to_learn: bool = True

    # 2. เรียกใช้ฟังก์ชันคำนวณ
    my_age = calculate_age(my_birth_year)

    # 3. แสดงผลด้วย f-string (เทคนิคยอดนิยมของ Python)
    # ใส่ตัว f หน้าเครื่องหมายคำพูด แล้วใส่ {ตัวแปร} ข้างในได้เลย
    print(f"สวัสดีครับ, ผมชื่อ: {my_name}")
    print(f"เกิดปี: {my_birth_year} (อายุประมาณ {my_age} ปี)")
    
    # 4. เช็คสถานะ (Logic พื้นฐาน)
    if is_ready_to_learn:
        print("สถานะ: 🚀 พร้อมลุย Full Stack!")
    else:
        print("สถานะ: 💤 พักผ่อนก่อน")