# smart_cart.py
# Feature: ระบบคำนวณตะกร้าสินค้า พร้อมเช็คส่วนลดและงบประมาณ

# --- 1. User Input (รับงบประมาณ) ---
print("🛒 --- Welcome to Smart Cart System (M1 Edition) ---")
raw_budget: str = input("กรุณาระบุงบประมาณของคุณ (บาท): ")
user_budget: float = float(raw_budget)

# --- 2. Data Setup (รายการสินค้าในตะกร้า) ---
# List เก็บราคาสินค้า (สมมติว่าหยิบมา 4 ชิ้น)
cart_prices: list[float] =[4500.00,2500.00,1200.00,990.00]
total_cost: float = 0.0

# --- 3. Loop Logic (วนลูปคำนวณยอดรวม) ---
print("\n📝 รายการสินค้า:")
for price in cart_prices:
    # แสดงรายการและบวกสะสมยอดเงิน
    print(f"    -{price:,.2f} THB")
    total_cost += price

print("-" * 30)
print(f"ยอดรวมสินค้า: {total_cost:,.2f} THB")

# --- 4. Business Logic (คำนวณส่วนลด) ---
# กฎ: ซื้อครบ 30,000 ลด 10%
discount: float = 0.0

if total_cost >30000:
    print("🎉 ยินดีด้วย! ยอดซื้อเกิน 30,000 รับส่วนลด 10%")
    discount = total_cost * 0.10
else:
    print("(ซื้อไม่ถึงเกณฑ์ ไม่ได้ส่วนลด)")

net_price: float = total_cost - discount

# --- 5. Final Decision (เช็คงบประมาณ) ---
print("-" * 30)
print(f"💰 ยอดสุทธิ:  {net_price:,.2f} THB")
print(f"💳 งบของคุณ:     {user_budget:,.2f} THB")

if user_budget >=net_price:
    change: float = user_budget - net_price
    print(f"✅ Approved! เงินทอน: {change:,.2f} THB")
else:
    missing: float = net_price - user_budget
    print(f"❌ Declined! เงินขาด:    {missing:,.2f} THB")
print("-" * 30)