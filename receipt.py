# receipt.py
# Reference Flowchart: Flow_ใบเสร็จร้านค้า.drawio

# --- [Node 1: Input] รับข้อมูล (Strict Type Hinting) ---
# : str   -> บอกให้โลกรู้ว่าเป็น "ข้อความ" เท่านั้น
# : float -> ราคาอาจมีทศนิยม (เช่น 99.50) ควรใช้ float
# : int   -> จำนวนชิ้นต้องเป็นจำนวนเต็มเท่านั้น (ขายครึ่งชิ้นไม่ได้)

product_name: str = "MacBook Pro M1"
price_per_unit: float = 45000.00
quantity: int =2 

# --- [Node 2: Process] คำนวณราคารวม ---
# ผลลัพธ์จากการเอา float * int จะได้ float เสมอ
total_price: float = price_per_unit * quantity

# --- [Node 3: Output] ออกใบเสร็จรายการ ---
print("-" * 30)
print("📄 RECEIPT / ใบเสร็จรับเงิน")
print("-" * 30)

# f-string ทำงานร่วมกับ Type Hint ได้ปกติ
print(f"สินค้า:   {product_name}")

# {:,.2f} -> ใส่ลูกน้ำ (,) และบังคับทศนิยม 2 ตำแหน่ง (.2f)
print(f"ราคา:   {price_per_unit:,.2f} บาท")
print(f"จำนวน:   {quantity} ชิ้น")

print("-" * 30)
print(f"ยอดรวม:     {total_price:,.2f} บาท")
print("-" * 30)